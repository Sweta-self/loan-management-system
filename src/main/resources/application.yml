spring:
  datasource:
    url: jdbc:mysql://localhost:3306/loan_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: my-secret-key-my-secret-key-my-secret-key
  expiration: 3600000